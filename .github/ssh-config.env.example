# SSH Configuration for TogetherOS Deployment
# Copy to .github/ssh-config.env and update values
# Do NOT commit the actual .env file

# VPS Connection Settings
VPS_HOST=72.60.27.167
VPS_USER=root
VPS_PATH=/var/www/togetheros

# SSH Key Settings (stored in GitHub Secrets)
# VPS_SSH_PRIVATE_KEY - Private key for deployment
# Generate with: ssh-keygen -t ed25519 -C "github-actions@togetheros"

# Notes:
# - Use IP address, not hostname aliases (more stable)
# - Current user is 'root' (deploy user planned for future)
# - See docs/dev/ssh-key-rotation.md for key management
